<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "POIs Management mini-app" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
					<%= link_to t('app.name'), root_path, :class => "brand" %>
          <div class="container-fluid nav-collapse">
						<ul class="nav">
							<li class="<%= 'selected' if controller_name.eql?("places") %>">
								<%= link_to t('app.navbar.sections.places'), places_path %>
							</li>
							<li class="<%= 'selected' if controller_name.eql?("exports") %>">
							<%= link_to t('app.navbar.sections.exports'), exports_path %>
							</li>
						</ul>
					</div>
        </div>
      </div>
    </div>

    <div class="container">
			<% if !notice.blank? %>
				<div class="alert alert-info"><%= notice %></div>
			<% elsif !alert.blank? %>
				<div class="alert alert-error"><%= alert %></div>
			<% end %>
      <div class="content">
        <div class="row">
        	<%= yield %>
        </div><!--/row-->
      </div><!--/content-->

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
		<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true"></script>
    <%= javascript_include_tag "application" %>

  </body>
</html>
